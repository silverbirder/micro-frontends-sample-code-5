{
    "name": "team-common-router-worker",
    "version": "1.0.0",
    "description": "package for creating workers templates",
    "main": "src/index.ts",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "format": "prettier --write '**/*.{js,css,json,md}'",
        "wrangler:dev": "wrangler dev --port 7100",
        "lt": "lt --port 7100 --subdomain silverbirder7100",
        "publish": "wrangler publish --env production"
    },
    "author": "author",
    "license": "ISC",
    "devDependencies": {
        "prettier": "^1.17.0",
        "localtunnel": "^2.0.0",
        "ts-loader": "^7.0.5",
        "typescript": "^3.9.5",
        "@cloudflare/workers-types": "^2.0.0",
        "@types/node": "^14.11.8"
    },
    "dependencies": {
        "serverless-cloudflare-workers": "^1.2.0",
        "@cloudflare/wrangler": "^1.11.0"
    }
}
